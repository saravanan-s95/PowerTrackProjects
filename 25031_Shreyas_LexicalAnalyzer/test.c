#if 0
#include <stdio.h>

int main() {
    int sum = 3 + 2;
    int x = 5.5;

    int 1arr[5];
    
    if (sum > 0) {
        printf("Sum is positive: %d\n", sum);
    }

    for (int i = 0; i < 5; i++) {
        x = x * i;
    }

    return 0;
}
#endif